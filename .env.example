# Required on Vercel for files above the Function request limit.
BLOB_READ_WRITE_TOKEN=

# Enable direct browser-to-Blob uploads in deployed builds.
NEXT_PUBLIC_BLOB_UPLOADS_ENABLED=true

# Used by the daily cleanup endpoint configured in vercel.json.
CRON_SECRET=
